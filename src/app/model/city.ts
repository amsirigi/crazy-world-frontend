import { Country } from "./country";

export class City {
    constructor(
        id: number,
        name: string,
        district: string,
        population: number,
        country: Country
    ){}
}
